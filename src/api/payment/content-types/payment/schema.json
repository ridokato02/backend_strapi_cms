{
  "kind": "collectionType",
  "collectionName": "payments",
  "info": {
    "singularName": "payment",
    "pluralName": "payments",
    "displayName": "Payment"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "order_id": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::order.order",
      "inversedBy": "payment"
    },
    "method": {
      "type": "enumeration",
      "enum": [
        "cod",
        "credit_card",
        "paypal",
        "bank_transfer",
        "momo",
        "vnpay"
      ]
    },
    "transaction_id": {
      "type": "text"
    },
    "amount": {
      "type": "decimal"
    },
    "status_payment": {
      "type": "enumeration",
      "enum": [
        "pending",
        "successful",
        "failed",
        "refunded"
      ]
    },
    "paid_at": {
      "type": "datetime"
    }
  }
}
