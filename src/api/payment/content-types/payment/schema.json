{
  "kind": "collectionType",
  "collectionName": "payments",
  "info": {
    "singularName": "payment",
    "pluralName": "payments",
    "displayName": "Payment"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "method": {
      "type": "enumeration",
      "enum": [
        "cod",
        "credit_card",
        "paypal",
        "bank_transfer",
        "momo",
        "vnpay"
      ]
    },
    "transaction_id": {
      "type": "text"
    },
    "amount": {
      "type": "decimal"
    },
    "status_payment": {
      "type": "enumeration",
      "enum": [
        "pending",
        "successful",
        "failed",
        "refunded"
      ]
    },
    "paid_at": {
      "type": "datetime"
    },
    "order": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::order.order",
      "inversedBy": "payment_ids"
    },
    "users_permissions_user": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::users-permissions.user",
      "inversedBy": "payments"
    }
  }
}
